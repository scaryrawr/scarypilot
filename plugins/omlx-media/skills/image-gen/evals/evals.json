{
  "skill_name": "image-gen",
  "evals": [
    {
      "id": 1,
      "name": "generate-png-from-prompt",
      "prompt": "Generate a square PNG image of a friendly red robot holding a tiny sunflower on a clean white background. Use any available image generation model exposed by $OMLX_BASE_URL; do not ask me to pick the model. Save the artifact as generated-red-robot.png under the output directory you were given, then report the path and model used.",
      "expected_output": "A generated PNG image is saved in the eval output directory and the final answer identifies the output path and model used.",
      "files": [],
      "expectations": [
        "The agent uses the image-gen skill and invokes omlx_image when the native plugin tool is available, otherwise it uses the bundled script instead of inventing a custom image API workflow.",
        "The native tool discovers a capable model internally, or the legacy fallback selects an available image generation model from $OMLX_BASE_URL/v1/models/status.",
        "The agent calls omlx_image with a finished prompt, size, and explicit output path, or invokes uv run scripts/generate.py with equivalent arguments when the tool is unavailable.",
        "The run creates generated-red-robot.png in the eval outputs directory or clearly named equivalent PNG image artifact under that directory.",
        "The created artifact is a non-empty valid PNG image, verified with a tool such as file, python, or equivalent.",
        "The final answer reports the generated image path and the model used, without leaving generated images in the image-gen skill directory."
      ]
    },
    {
      "id": 2,
      "name": "edit-provided-base-image",
      "prompt": "Edit the provided base image by replacing the central blue square with a shiny red apple icon while preserving the green circle, orange triangle, grid, and border as much as possible. Use any available image edit-capable model exposed by $OMLX_BASE_URL; do not ask me to pick the model. Save the artifact as edited-red-apple.png under the output directory you were given, then report the path and model used.",
      "expected_output": "An edited PNG image derived from the provided deterministic base fixture is saved in the eval output directory and the final answer identifies the output path and model used.",
      "files": [
        "evals/files/edit-base.png"
      ],
      "expectations": [
        "The agent uses the copied eval input image as the source image for the edit, not a newly generated replacement image.",
        "The native tool discovers a capable edit model internally, or the legacy fallback selects an available image edit model from $OMLX_BASE_URL/v1/models/status.",
        "The agent calls omlx_image with the copied source, a preservation-aware prompt, and explicit output path, or invokes uv run scripts/edit.py with equivalent arguments when the tool is unavailable.",
        "The run creates edited-red-apple.png in the eval outputs directory or clearly named equivalent PNG image artifact under that directory.",
        "The created artifact is a non-empty valid PNG image, verified with a tool such as file, python, or equivalent.",
        "The final answer reports the edited image path and the model used, without leaving edited images in the image-gen skill directory."
      ]
    }
  ]
}
