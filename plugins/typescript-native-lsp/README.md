# typescript-native-lsp

TypeScript/JavaScript language server for Claude Code using [typescript-native](https://github.com/microsoft/typescript-go)

Please install the preview globally before using:

```sh
npm install -g @typescript/native-preview
```
